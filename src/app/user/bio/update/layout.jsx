export const metadata = {
  title: "Update Bio - Promo Pioneer",
  description: "Update Bio Page",
};

export default function UpdateBioLayout({ children }) {
  return <main className="min-h-screen w-full">{children}</main>;
}
